{
  "name": "@voiden/plugin-voiden-mcp-tool",
  "version": "0.2.1",
  "type": "module",
  "private": false,
  "description": "Tool declaration block for .void files — mark an existing request as a named, typed capability an AI agent can call directly.",
  "main": "./src/plugin.ts",
  "scripts": {
    "build": "node build.mjs",
    "release": "node build.mjs && node generate-manifest.mjs"
  },
  "peerDependencies": {
    "@voiden/sdk": "1.0.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "yaml": "^2.4.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@voiden/sdk": "1.0.10",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
